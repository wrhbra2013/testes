# Rust Tests

## Recommended Frameworks

- **Built-in `#[test]`** - https://doc.rust-lang.org/book/ch11-01-writing-tests.html
- **proptest** - Property-based testing
- **criterion** - Benchmarks

## Structure

Tests can be in `src/` as `#[cfg(test)]` modules or in `tests/` directory.
